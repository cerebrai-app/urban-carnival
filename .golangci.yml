version: "2"

linters:
  default: standard
  enable:
    - bodyclose
    - errorlint
    - gocritic
    - misspell
    - revive
    - unconvert
    - unparam
  exclusions:
    presets:
      - comments
      - common-false-positives

formatters:
  enable:
    - gofmt
    - goimports
